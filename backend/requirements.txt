# Runtime dependencies only. Anything not imported by backend/app is deliberately absent:
# unused wheels (psycopg, sqlalchemy) meaningfully slow cold builds on small hosts.
fastapi
uvicorn[standard]
python-multipart
pydantic
pydantic-settings
python-dotenv

# Database (Supabase REST client; no direct Postgres driver needed)
supabase

# AI workflow + providers
langgraph
langchain-core
langchain-groq

# Embeddings (pulls onnxruntime and numpy transitively)
fastembed

# Document parsing
pypdf
python-docx

# HTTP client used for the Gemini and Ollama providers
httpx
